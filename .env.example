REACT_APP_API_BASE=
# Required for demo session DB logging — must be present when the host runs `vite build`
# (values are inlined at build time, not read from the server at runtime).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Optional: on staging, set true then inspect window.__vynqeSessionLog after a demo completes
VITE_DEBUG_SESSION_LOG=
VITE_FORMSPREE_BOOK_DEMO=
VITE_FORMSPREE_DEMO_END=
VITE_GA_MEASUREMENT_ID=

